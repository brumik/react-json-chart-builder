import onClick from './onClick';
import axisFormat from './axisFormat';
import labelFormat from './labelFormat';
export * from './types';

export default {
    onClick,
    axisFormat,
    labelFormat
};